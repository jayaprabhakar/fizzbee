
state:
  ELEMENTS = set([1, 2, 3, 4, 5])
  elements = set([])
  count = 0

action Add:
   any e in (ELEMENTS - elements):
     elements.add(e)
     count += 1

action Remove:
   any e in elements:
     elements.remove(e)
     count -= 1

